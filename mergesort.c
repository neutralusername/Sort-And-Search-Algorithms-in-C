#include <stdio.h>
#include "array_functions.h"

void mergeSortArray(int *array, int length, int ascending) {
    printf("mergesort - implementation missing!\n\narray:\n");
    printArray(array, length);
    if(ascending) {
        printf("\n\nascending");
    } else {
        printf("\n\ndescending");
    }
}